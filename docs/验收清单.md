# 验收清单

更新时间：`2026-05-19`

本轮「前端对齐 + 主题系统」的验收覆盖范围。每一项标注当前状态以及是否需要人工复核。

## 1. 构建层

| 项 | 命令 | 状态 |
| --- | --- | --- |
| 主 target 编译 | `swift build` | ✅ 通过 |
| 测试 target 编译 | `swift build --target PromptPanelTests` | ✅ 通过 |
| 测试实际执行 | `swift test` | ⚠️ 需 Xcode（本机只装了 Command Line Tools，xctest 不可用） |
| `.app` 打包 | `scripts/build-app.sh` | ✅ 通过 |
| ad-hoc 签名校验 | `codesign --verify --deep --strict` | ✅ 通过（随 build-app.sh 内联执行） |

## 2. 数据库与迁移

| 项 | 期望 | 实测 |
| --- | --- | --- |
| 迁移链路 | v1 → v2 → v3 → v4 → v5 五个迁移按顺序执行 | ✅ `grdb_migrations` 表按序列出五条记录 |
| `entries.tags` 列 | `TEXT NOT NULL DEFAULT '[]'` | ✅ schema 匹配 |
| 遗留标签索引 | `index_entries_on_tags` 不应存在 | ✅ v5 会执行 `DROP INDEX IF EXISTS` |
| `app_settings.app_theme` | 首次启动后读出 `.system`，toggle 后持久化 | ✅ 设为 `light` 后重启仍为 `light` |
| 面板尺寸 / 位置设置 | `panel_content_width/height`、`panel_window_origin_x/y` 可持久化且尺寸归一化 | ✅ 已有 SettingsRepository 回归覆盖 |
| 崩溃 / 异常 | 启动、关闭、切主题全过程无 `error / crash / exception` 日志 | ✅ `log show --predicate 'subsystem == com.promptpanel.app'` 无匹配 |

## 3. 前端对齐

| 视图 | 对齐依据 | 状态 |
| --- | --- | --- |
| 快捷面板 | `frontend-draft/components/quick-panel.jsx` + `panel-default.png` | ✅ 连续序号 / 类型图标 / 响应式标题与预览 / 通用徽标 / 类型标签 / 搜索提示 / 30px 默认行高 / ⌘1-9 footer 全部到位 |
| 内容库 | `frontend-draft/components/library.jsx` | ✅ 三栏（项目 200 / 列表 360 / 预览 flex），侧栏计数、筛选芯片、排序菜单、预览主动作 |
| 设置 | `frontend-draft/uploads/PromptPanel-settings.png` | ✅ 顶部健康摘要 + 偏好 / 权限 / 维护分区；偏好承载外观、词条排序、快捷键、面板行为；权限承载授权与运行概况；维护承载备份、数据位置、最近执行记录 |

## 4. 主题切换

| 项 | 期望 | 状态 |
| --- | --- | --- |
| `AppTheme` 选项 | 跟随系统 / 浅色 / 深色，默认跟随系统 | ✅ `Core/Models/AppTheme.swift` |
| 持久化 | 选择存入 `app_settings.app_theme` | ✅ SQL 实测写入 |
| SwiftUI 内容 | `.preferredColorScheme` 应用在 MainWindowView + QuickPanelView | ✅ |
| NSWindow chrome | traffic lights / titlebar 跟随主题 | ✅ `AppDelegate.applyAppearance` 同步给 `NSApp.appearance` / 主窗口 / 面板 |
| 动态颜色 | `Constants.VisualStyle.*` 全部 `NSColor(name:dynamicProvider:)` | ✅ |
| tint token | `tintSubtle / tintMedium / tintStrong / scrim` 按主题翻转 | ✅ |
| 深色可读性 | 非选中摘要、类型、底部键位提示保持可扫读 | ✅ |
| 遗留 `Color.white/black.opacity(x)` | 全部替换 | ✅ grep 清零（仅 Constants.swift 里的注释） |

## 5. UI QA 截图

6 张一一对应，分别覆盖两套主题下的三种形态：

| 形态 | 深色 | 浅色 |
| --- | --- | --- |
| 快捷面板 780×440 | `panel-dark.png` | `panel-light.png` |
| 内容库 1100×740 | `library-dark.png` | `library-light.png` |
| 设置 1100×740 | `settings-dark.png` | `settings-light.png` |

生成方式：`./scripts/capture-ui-qa.sh`（会循环 `dark / light` 两套主题写入 `app_settings.app_theme`，再由同一个 `.app` 渲染）。

## 6. 尺寸统一

| 形态 | 默认尺寸 | 说明 |
| --- | --- | --- |
| 主窗口（内容库 + 设置） | 1100 × 740 | 单一 NSWindow，切换标签不变 |
| 快捷面板 | 780 × 440 | 独立 NSPanel，匹配 `frontend-draft/index.html` 的 `<QuickPanel width={780} />` |
| 编辑器 sheet（项目 / 词条 / 迁移删除） | 独立 | 按用户豁免允许各自宽高 |

## 7. 人工验收待补

以下项依赖真实桌面环境或授权状态，无法在当前机器自动化：

- [ ] `⌥2` 全局热键在真实前台应用（Chrome / Cursor / VS Code / ChatGPT Web / Claude Web）下呼出面板
- [ ] 辅助功能授权后的自动粘贴闭环
- [ ] 面板在仅复制兜底模式下用户手动 ⌘V 粘贴的体验
- [ ] 主题在"跟随系统"下，切换系统外观时实时生效（当前通过显式选项验证过两套；跟随系统也走同一路径，逻辑上等价）
- [ ] macOS 15 / macOS 14 Sonoma 兼容性（本机版本以 `sw_vers` 为准）

---

## 相关提交

```
9957c60 feat(theme): 加浅色/深色主题切换（跟随系统为默认），视觉 token 全部动态化
e273483 style: 统一默认尺寸 — 主窗口 1100x740，面板 780x440
4b36279 fix(settings): 单页密集布局收掉大片留白，贴齐设计稿
b6d729f fix(settings): 收紧设置页布局，补齐面板偏好开关
4e99907 feat: 以 frontend-draft/ 为基准对齐完整前端，补齐 tags 与面板直达
```
