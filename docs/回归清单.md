# PromptPanel 主链路回归清单

更新时间：`2026-05-09`

## 每次发版前至少检查

- [x] `./scripts/build-app.sh` 可生成新的 `dist/PromptPanel.app`
- [x] `./scripts/release-readiness.sh` 可跑通当前机器支持的全部发布前检查
- [x] 产物内包含 `PromptPanel_PromptPanel.bundle` 等资源 bundle，不是空壳 `.app`
- [x] 新包可直接启动，不出现 `Info.plist` / 资源丢失 / 权限说明缺失
- [x] release `.app` 可通过 `codesign --verify --deep --strict`
- [x] zip 产物解压后仍可通过签名校验
- [x] 使用隔离数据目录启动时，会创建数据库与启动备份，且进程可稳定驻留
- [x] 全局快捷键可呼出面板
- [x] 冷启动后第一次按热键可打开
- [x] 面板呼出后搜索框自动聚焦
- [x] 再按一次全局快捷键可关闭面板
- [x] `Esc` 可关闭面板
- [ ] 点击面板外部可关闭面板
- [ ] 当前项目切换后，搜索结果立即刷新
- [ ] 当前项目与 `通用项目` 混排结果符合预期
- [x] 项目列表保持 `通用项目` 默认项目优先，其余项目按名称排序
- [x] 同权重混排/普通列表/FTS 词条使用 `updatedAt + id` 做最终兜底排序，刷新时不随机跳动
- [ ] 键盘上下移动选择正常
- [x] 回车执行正常
- [x] 鼠标单击词条执行正常
- [x] 快捷面板结果行、项目选择、固定、设置等高频控件按完整视觉区域响应点击
- [x] 无辅助功能权限时，进入仅复制模式且提示正确
- [ ] 已授权时，可在至少 3 个目标应用中完成自动粘贴
- [x] 自动粘贴失败时，剪贴板兜底仍可用
- [ ] 主界面新增词条后，面板能立即搜到
- [ ] 编辑词条后，面板结果立即更新
- [x] 内容库筛选类型/标签后，预览区不会继续显示已被过滤掉的旧词条
- [x] 内容库本地排序在使用次数/最近时间/标题相同时仍保持稳定兜底顺序
- [x] 内容库可见词条使用派生缓存，排序模式切换后缓存立即刷新，不在视图重绘中反复过滤排序
- [x] 内容库项目行、词条行、分段控件、搜索框空白区域和图标动作按完整视觉区域响应点击
- [ ] 删除空项目成功
- [ ] 删除非空项目时，迁移后结果正确
- [ ] 最近执行记录可看到目标应用、失败原因和耗时
- [ ] 清理 30 天前日志不会影响当前功能
- [ ] 至少记录 10 个“热键 -> 可输入”耗时样本
- [ ] 若任一样本超过 `300ms`，已记录场景和日志时间点
- [ ] 连续 20 次热键开关无卡死、无残留窗口、无明显延迟劣化
- [ ] 手动点击“立即备份”后，备份目录中能看到新增 `.sqlite`
- [x] 构造损坏库启动时，会隔离旧库并重建可用数据库
- [x] 构造迁移失败时，不会自动重建空库
- [x] 用 `./scripts/restore-backup.sh` 做一次回滚演练时，不会覆盖掉旧现场
- [x] `./scripts/restore-backup.sh` 会拒绝损坏备份
- [x] `./scripts/restore-backup.sh` 会先 staging 并校验备份副本，再移动当前数据库现场
- [x] 文档体系已覆盖架构、部署、配置、关键模块、运维排错、接手维护和文档同步规则
- [ ] 每次代码 / 脚本 / UI 基准变更后，按 [文档与代码同步矩阵](./文档与代码同步矩阵.md) 复查对应文档

## 当前已完成的隔离 QA 样本

- [x] 使用隔离目录启动应用，不污染真实 `~/Library/Application Support/PromptPanel`
- [x] 冷启动后二次确认新默认热键为 `⌥2`
- [x] 只按热键不会误触发执行
- [x] 呼出后等待约 `0.2s` 再按回车可执行词条
- [x] 连续 20 次热键开关无误执行、无进程退出
- [ ] 真实目标应用自动粘贴首轮样本
- [ ] Finder / Chrome / Cursor 或 VS Code 兼容性样本
- [ ] 完整 Xcode / `xctest` 环境下的真实测试执行
- [x] Chrome 前台下热键呼出后稳定拿到焦点并可回车执行
- [x] 打包 `.app` 已修正 `CFBundleExecutable`，可被系统正常打开
- [x] release 打包脚本会把 SwiftPM resource bundle 放入 `Contents/Resources`
- [x] 未传正式证书时，打包产物会做 ad-hoc 签名，避免本机验证包处于明显无效签名状态
- [x] Chrome textarea 样本在未授权辅助功能时可稳定进入仅复制模式，并写入 execution log
- [x] 2026-04-19 真实桌面实测：使用系统级 `⌥2` 可在 Chrome 前台拉起面板，再按一次 `⌥2` 可关闭，`Esc` 也可关闭
- [x] 2026-05-02 本地发布门禁通过：`./scripts/release-readiness.sh --allow-build-only-tests` 完成打包、签名、隔离启动冒烟和恢复演练
- [x] 2026-05-02 自动化覆盖：鼠标单击可直接执行对应可见词条并写入 `pointer_click` 执行日志

## 异常路径重点检查

- [ ] 主窗口顶部 header 为实底，不透出系统 titlebar 或桌面背景
- [ ] 设置页顶部状态横幅为实底，不透出底层卡片或背景
- [ ] 未授权时反复呼出，不会卡死或重复弹系统权限引导
- [ ] 面板快速开关多次，不会丢焦点或残留悬浮窗
- [ ] 主窗口打开期间，快捷面板仍可正常呼出
- [x] 目标应用焦点未恢复时，会进入复制兜底并留下可诊断日志
- [x] 故意构造一次迁移失败时，应用会保留原始数据库并直接报错，不会重建空库
