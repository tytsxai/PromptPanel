# 空间清理记录（2026-05-07）

## 本次目标

在不影响现有功能、构建流程和运行环境的前提下，识别并删除确认可再生、过期或本地临时性质的低风险内容，释放本地仓库占用空间。

## 已确认并执行删除

| 路径 | 删除前体积 | 性质 | 风险判断 |
| --- | ---: | --- | --- |
| `.build/` | 约 1.31 GiB | Swift Package Manager 构建缓存、依赖 checkout、编译中间产物 | 不纳入 Git；`swift build` / 打包脚本可重新生成 |
| `dist/` | 约 61.6 MiB | 本地 `.app`、zip、release-readiness、UI QA 打包输出 | 当前运行的应用来自 `/Applications/PromptPanel.app`，不是 `dist/`；脚本会重新生成 |
| `.code-review-graph/` | 约 160 KiB | 本地代码审查/分析图数据库 | 不参与构建、运行和发布；可按需重新生成 |
| `docs/ui-qa/latest/` | 约 3.7 MiB | 2026-05-02 生成的窗口截图快照 | 未被文档引用；`scripts/capture-ui-qa.sh` 可重新生成 |

本次删除前，上述目标合计约 `1.37 GiB`。

## 本次明确保留

| 路径 | 保留原因 |
| --- | --- |
| `Package.resolved` | 虽然当前被 `.gitignore` 忽略，但它固定 SwiftPM 依赖解析结果；删除可能引入后续依赖漂移 |
| `scripts/icon/master_1024.png`、`scripts/icon/AppIcon.icns`、`scripts/icon/AppIcon.iconset/` | 图标刷新流程的生成素材；体积小，删除收益低 |
| `docs/` 主文档 | 当前已作为随仓库版本化的交接资料；不能按临时目录整体清理 |
| `docs/ui-qa/generic-input-harness.html` | UI QA 辅助页面，不是过期截图产物 |
| `frontend-draft/` | 当前有未提交改动，且是 UI 基准资料；不属于低风险可清理内容 |
| `.claude/` | 本地工具配置，体积极小且用途未失效 |

## 已验证

清理后已确认：

1. `git status --short --branch` 未出现由清理引入的源码/配置删除；仓库已有未提交改动保持原样。
2. `zsh -n scripts/build-app.sh`、`zsh -n scripts/release-readiness.sh`、`zsh -n scripts/capture-ui-qa.sh` 通过。
3. `swift build --package-path . --build-path /tmp/promptpanel-cleanup-build -c debug` 通过，说明 `.build/` 删除后仍可从源码和依赖缓存恢复构建。
4. 验证产生的 `/tmp/promptpanel-cleanup-build` 和仓库内重新出现的 `.build/` 残留已再次删除。

## 后续清理边界

- 可以周期性清理：`.build/`、`dist/`、`.code-review-graph/`、`docs/ui-qa/latest/`。
- 不要为了执行 `git clean -fdX` 而误删：`scripts/icon/` 生成图标、`Package.resolved`；`docs/` 现在应由 Git 状态显式保护。
- 如果需要清理 UI QA 资料，优先只清 `docs/ui-qa/latest/` 的截图，不要删除 `docs/ui-qa/generic-input-harness.html`。
